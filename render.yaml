
services:
  - type: web
    name: recycle-form
    env: python
    buildCommand: ""
    startCommand: gunicorn app:app
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: recycle-db
          property: connectionString

databases:
  - name: recycle-db
